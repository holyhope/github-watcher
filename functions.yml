functions:
  github_watch:
    runtime: bash
    handler: main.run
    environment:
      INTERVAL_EXEC_HOURS: 24
      USERNAME: "${env:USERNAME}"
      API_TOKEN: "${env:API_TOKEN}"
      POST_URL: "https://${POST_CREDENTIALS}@elasticsearch:9200/github-watcher/result"
#    events:
#      - schedule:
#          rate: cron(0 0 * * *)
